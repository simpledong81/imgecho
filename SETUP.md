# ImgEcho - 快速设置指南

## 🎯 5 分钟快速上手

### 步骤 1: 安装 Node.js

确保你已经安装了 Node.js 18 或更高版本。

**检查版本：**
```bash
node --version  # 应该显示 v18.0.0 或更高
npm --version   # 应该显示 9.0.0 或更高
```

**没有 Node.js？**
- 访问 [nodejs.org](https://nodejs.org/) 下载并安装 LTS 版本
- 或者使用 [nvm](https://github.com/nvm-sh/nvm) 来管理多个 Node 版本

---

### 步骤 2: 安装依赖

在项目根目录运行：

```bash
npm install
```

这会安装所有必需的依赖包，包括：
- Vite（开发服务器和构建工具）
- ESLint（代码质量检查）
- Prettier（代码格式化）
- exif-js（EXIF 数据解析）
- terser（代码压缩）

---

### 步骤 3: 启动开发服务器

```bash
npm run dev
```

浏览器会自动打开 `http://localhost:3000`，你可以立即看到应用运行效果！

**开发服务器特性：**
- ⚡ 热模块替换（HMR）- 修改代码后自动刷新
- 🚀 快速启动 - Vite 提供极速的开发体验
- 📱 局域网访问 - 可以在手机上测试（显示的 Network URL）

---

### 步骤 4: 开始开发

1. **修改代码**
   - 编辑任何 `.js`、`.css` 或 `.html` 文件
   - 保存后浏览器自动刷新

2. **检查代码质量**
   ```bash
   npm run lint        # 检查代码规范
   npm run lint:fix    # 自动修复问题
   ```

3. **格式化代码**
   ```bash
   npm run format      # 格式化所有文件
   ```

---

### 步骤 5: 构建生产版本

当你准备部署时：

```bash
npm run build
```

构建输出会生成在 `dist/` 目录，包含：
- 压缩和优化的 JavaScript 文件
- 压缩的 CSS 文件
- 优化的 HTML 文件
- 所有静态资源

**预览生产构建：**
```bash
npm run preview
```

在 `http://localhost:8080` 查看生产版本的实际效果。

---

## 📋 常用命令速查

| 命令 | 说明 |
|------|------|
| `npm install` | 安装所有依赖 |
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 构建生产版本 |
| `npm run preview` | 预览生产构建 |
| `npm run lint` | 检查代码规范 |
| `npm run lint:fix` | 自动修复代码问题 |
| `npm run format` | 格式化代码 |

---

## 🛠️ IDE 设置（可选但推荐）

### Visual Studio Code

1. **安装推荐插件：**
   - ESLint
   - Prettier - Code formatter
   - EditorConfig for VS Code
   - Vite

2. **配置自动格式化：**

   在 VS Code 设置（`settings.json`）中添加：
   ```json
   {
     "editor.defaultFormatter": "esbenp.prettier-vscode",
     "editor.formatOnSave": true,
     "editor.codeActionsOnSave": {
       "source.fixAll.eslint": true
     }
   }
   ```

---

## 🔧 故障排查

### 问题 1: 端口被占用

**错误信息：** `Port 3000 is already in use`

**解决方案：**
```bash
# 方法 1: 杀死占用端口的进程
lsof -ti:3000 | xargs kill -9

# 方法 2: 使用不同的端口
# 修改 vite.config.js 中的 port 配置
```

### 问题 2: 依赖安装失败

**解决方案：**
```bash
# 清除缓存并重新安装
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

### 问题 3: 构建失败

**解决方案：**
```bash
# 确保所有依赖都已正确安装
npm install

# 检查 Node 版本
node --version  # 应该 >= 18.0.0

# 重新构建
npm run build
```

### 问题 4: ESLint 报错

**解决方案：**
```bash
# 自动修复大部分问题
npm run lint:fix

# 如果仍有错误，手动修复或临时禁用该规则
```

---

## 🌐 部署到 GitHub Pages

### 方法 1: 使用 GitHub Actions（推荐）

项目已配置好 GitHub Actions，推送到 `main` 分支会自动部署。

1. 确保 GitHub Pages 已启用：
   - 仓库 Settings → Pages
   - Source: GitHub Actions

2. 推送代码：
   ```bash
   git push origin main
   ```

3. 等待 Actions 完成，访问你的 GitHub Pages 网址。

### 方法 2: 手动部署

```bash
# 构建
npm run build

# 安装 gh-pages 工具（一次性）
npm install -g gh-pages

# 部署到 gh-pages 分支
gh-pages -d dist
```

---

## 📚 下一步

- 阅读 [README.dev.md](README.dev.md) 了解详细的开发指南
- 阅读 [CONTRIBUTING.md](CONTRIBUTING.md) 了解如何贡献代码
- 查看 [GitHub Issues](https://github.com/simpledong81/imgecho/issues) 了解待实现的功能

---

## 🆘 需要帮助？

- **GitHub Issues**: [提交问题](https://github.com/simpledong81/imgecho/issues)
- **文档**: 查看项目根目录的其他 Markdown 文件
- **示例**: 运行 `npm run dev` 并加载示例图片

---

**祝你开发愉快！如果遇到问题，随时在 GitHub Issues 提问。** 🎉
